import React, {useEffect, useState} from 'react';
import Header from "../includes/Header";

const Profile = (props) => {

    return (
        <div>
            <Header />

        </div>
    );
};

export default Profile;
